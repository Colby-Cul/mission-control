'use client'
/**
 * OwnershipWizard — stub placeholder for the ownership setup wizard.
 * Full implementation is handled via the EntityOrgChart + EditOwnershipModal flow on /entities.
 */
export default function OwnershipWizard() {
  return (
    <div style={{ padding: '48px 32px', textAlign: 'center', color: 'rgba(255,255,255,0.5)', fontSize: 14 }}>
      Ownership setup wizard — use the Entities page to manage ownership structure.
    </div>
  )
}
